{
  "name": "taskflow-backend",
  "version": "1.0.0",
  "description": "API para gerenciamento de tarefas com Docker",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "docker-build": "docker build -t taskflow-backend .",
    "docker-run": "docker run -p 3002:3002 taskflow-backend"
  },
  "dependencies": {
    "express": "^4.18.2",
    "mongoose": "^7.5.0",
    "cors": "^2.8.5"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}