version: 1

services:
  - type: web
    name: taskflow-app
    env: node
    plan: free
    region: ohio
    buildCommand: |
      cd backend && npm install
    startCommand: |
      cd backend && node server.js
    envVars:
      - key: NODE_ENV
        value: production
    healthCheckPath: /health